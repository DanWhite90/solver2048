mod ai;

/*
The public API should expose:
    - a method for passing a state of the grid and return an optimal move
*/

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
